name: inventario_qr
description: Sistema de inventario multi-artículo bajo modelo QR con restricciones
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # FluentUI para Windows Desktop
  fluent_ui: ^4.12.0
  # Gestión de estado
  provider: ^6.1.5
  # Manejo de Excel
  excel: ^4.0.6
  file_picker: ^10.2.0
  path_provider: ^2.1.2
  # Abrir archivos
  open_file: ^3.5.10
  # Google Fonts
  google_fonts: ^6.3.0
  # Gráficas para visualización de datos
  fl_chart: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
